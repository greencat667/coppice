# Flags

> Written by the morning briefing. One row per item it's currently flagging, so a flag repeated day after day escalates instead of becoming background noise (rule 22). Remove a row when the item is done, parked or dropped.
>
> **Rungs:** 1 = flag it · 2 = propose one specific unsticking action · 3 = ask whether to park it or drop it. An item moves up a rung after it has been flagged on `budgets.stale_flag_days` briefings in a row at its current rung.

| Item | First flagged | Last flagged | Times at this rung | Rung | Note |
|---|---|---|---|---|---|
