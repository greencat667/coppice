# Context

Reference material the assistant reads on demand: strategy documents, briefs, policies, style guides, templates. Name files so their purpose is obvious from the name alone, e.g. `style-guide-reports.md`, not `notes2.md`.

`context/coppice/` holds Coppice's own docs (rules, layers, patterns). The setup skill copies them here so the assistant can find them offline.
